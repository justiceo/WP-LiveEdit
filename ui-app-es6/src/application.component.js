class AppController { 
}

const appComponent = {
  template: `
        <div id="le_toolbar" class="toolbar-wrapper bottom">
          <p>toolbar works</p>
          <toolbar></toolbar>
        </div>
    `,
  controller: AppController
};

export default appComponent;
