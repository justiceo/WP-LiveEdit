class AppController { 
}

const appComponent = {
  template: `
        <div class="container-fluid">
          <toolbar></toolbar>
        </div>
    `,
  controller: AppController
};

export default appComponent;
